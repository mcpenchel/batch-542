// Variables

let age = 32;
age += 1;

const name = "Matheus";
// name = "Matheus Charif" => explode an error

const students = [];
students.push("Marta");

// Declaring a function

const myFunction = () => {
  return "some value";
}

// Loops

const ourStudents = ["Marta", "David", "Alston"];

ourStudents.forEach((element) => {
  console.log(`The student is ${element}`);
});

// Conditions

if (condition) {

} else if (some other condition) {

} else {

}